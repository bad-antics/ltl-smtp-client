// examples/demo.ltl — runnable demo for ltl_smtp_client

import std::io
use ltl_smtp_client as lib

fn main() {
    io::println("ltl_smtp_client demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}
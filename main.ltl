// ltl-smtp-client/main.ltl
//
// SMTP client with STARTTLS, AUTH PLAIN/LOGIN, and attachments.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-smtp-client v0.1.0")
    io::println("SMTP client with STARTTLS, AUTH PLAIN/LOGIN, and attachments.")
    0
}
